[package]
name = "stoffbuch-gate"
version = "0.0.0"
description = "The gate: every check that guards the register, run in order."
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
stoffbuch = { path = "../stoffbuch" }
stoffbuch-schema = { path = "../stoffbuch-schema" }

[lints]
workspace = true
